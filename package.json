{
  "name": "urlfetchingservice",
  "version": "1.0.0",
  "description": "Service to fetch an array of URLs and return their response as a Promise",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Amir Arreaza",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
